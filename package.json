{
  "name": "background-audio-loop-saas",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "pnpm --filter server start",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.1.2",
    "typescript": "^5.3.3"
  },
  "packageManager": "pnpm@9.0.0"
}
